<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="MPProgressButton">
        <attr name="mp_text" format="string|reference" />
        <attr name="mp_textColor" format="color|reference"/>
    </declare-styleable>

    <declare-styleable name="MPProgressText">
        <attr name="mp_text"/>
        <attr name="mp_textColor"/>
    </declare-styleable>

    <declare-styleable name="MPSwitchView">
        <attr name="mp_text" />
        <attr name="mp_textColor"/>
        <attr name="mp_checked" format="boolean|reference" />
    </declare-styleable>

    <declare-styleable name="MPTextInputEditText">
        <attr name="android:hint" format="string" />
        <attr name="android:inputType"/>
        <attr name="android:maxLength" />
        <attr name="android:maxLines" />
        <attr name="android:nextFocusDown" />
        <attr name="android:nextFocusRight"/>
        <attr name="mp_endOfString" format="boolean" />
    </declare-styleable>

</resources>
