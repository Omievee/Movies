<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moviepass">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".application.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyBYTdUkozXsZOpZ0WbJKkXYq_MIb9EBlXI" />

        <activity
            android:name=".activities.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/RedTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.BrowseActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activities.ConfirmationActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activities.ETicketsActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activities.LogInActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activities.MovieActivity"
            android:screenOrientation="portrait"
            android:theme="@style/MoviePassThemeTransparentStatusBar"/>

        <activity
            android:name=".activities.NotificationsActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activities.ProfileActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activities.SettingsActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activities.SignUpFirstOpenActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activities.SignUpStepOneActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activities.TheaterActivity"
            android:theme="@style/AppTheme">
        </activity>

    </application>

</manifest>